-- Default short-term obstacle configuration.
-- ShortTermObject currently ignores file contents, but this placeholder keeps
-- the expected path alive and documents the intended structure.
short_term_objects = {
  {
    pose = {x = 0.0, y = 0.0, theta = 0.0},
    shape = {
      type = "box",
      size = {x = 1.0, y = 1.0}
    }
  }
}
